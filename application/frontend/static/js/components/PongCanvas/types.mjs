/**
 * @typedef {number} VCH
 * number between 0 and 100
 * VCH = View Canvas Height
 * It should work like a `vh` unit but based on the canvas height
 *
 * Its basically a number representing the percentage based on the height of the element
 */

/** @typedef {number} VCW
 * number between 0 and 100
 * VCW = View Canvas Width
 * It should work like a `vw` unit but based on the canvas width
 *
 * Its basically a number representing the percentage based on the width of the element
 */
